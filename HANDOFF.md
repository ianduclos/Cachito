---
project: Cachito
updated: 2026-08-28
entries: 2
---

### Forced escalation — design drafted, awaiting Ian's review — opened 2026-08-28, owner: ian
- done: Diagnosed AND designed. Mechanism: `src/bot/champion/beliefEquity.ts:416-440` decides Dudo-vs-raise as `evDudo > evBestBid` with `evBestBid = equityNow` — the same number whether the best available raise is supported or hopeless — so a cornered bot raises without noticing. The bluff is the residue of that comparison, and it bypasses `personaBluff.ts`'s deliberate-bluff machinery entirely. Note this was a RATIFIED scope boundary (`beliefEquity.ts:40-43`), not a bug; the packet crosses it deliberately. Brainstorm complete: Ian chose to keep the bluff RATE and make it believable (not to bluff less), named the three tells (timing, raise size, face chosen — explicitly not repetition), and chose a cornered-ness detector as the gate over an exploiter bot or a human-read reel. Spec at `docs/superpowers/specs/2026-08-28-forced-escalation-design.md` (`b5a157a`).
- next: **Ian reviews the spec, fresh.** On approval, invoke the `writing-plans` skill to turn it into an implementation plan — do NOT start coding off the spec directly. Phase 1 of the plan changes no bot code: run the detector against the UNCHANGED champion to establish the baseline, because if it comes back near chance the premise is wrong.
- blockers: Ian's review. One item in the spec is explicitly marked an OPEN ASSUMPTION rather than a decision — section 2's layering mismatch (Gen 2 prices raise-vs-challenge against its own best bid, then the persona substitutes a smaller one, so the comparison is against a bid never played). He rubber-stamped it late in a long session; it needs a considered yes or a different answer.
- context: The spec is self-contained. Supporting evidence in `lab/ROADMAP.md` ("NEXT PACKET — forced escalation") and the `lab/LOG.md` 2026-08-28 (last) entry. The standing constraint that shapes everything: self-play cannot measure believability (memory `self-play-cannot-measure-believability`), so a duel must never be the opening move and win-share non-inferiority is a floor, never the evidence.

### Table-dice read-back — approach replaced, work not started — opened 2026-08-28, owner: claude
- done: The decision is made (active@leakage 1.5, ratified by Ian) and every measurable blocker is discharged, including the fresh-seed confirmation on block 810001. Tonight established two things that change the remaining work: the production edit is a SINGLE site (the `random() < tableDiceChance` branch in `src/bot/champion/personaBluff.ts`, ~line 289 — all three `toldStory` branches feed it, and the `base.choice.tableDiceIndices` pass-through is dead because Gen 2 never emits reveals), and asking Ian to "play against it" does not work.
- next: Implement active@1.5 at that branch, re-duel with the production policy as the candidate to confirm it reproduces the lab arm, then build the reveal reel — one page of ~15 generated reveal moments, current coin-flip bot alongside active@1.5, so the legibility judgement takes two minutes instead of an hour of play.
- blockers: none for the implementation. The read-back itself still needs Ian, but as a page to skim rather than games to play.
- context: `lab/notes/table-dice-reveal-decision.md` (decision, evidence, promotion path, and the single-site verification). Artifacts in `lab/data/living-v5/`. The read-back rationale is in STATUS.md's 2026-08-28 (fourth) entry: Ian played 27 rounds, reported on Calzos and bid ladders in detail, and never mentioned table dice — there were 5 bot reveals in that game, and that invisibility is the argument for the change.
